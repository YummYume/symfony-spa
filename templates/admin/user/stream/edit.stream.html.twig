{% include "common/stream/alert.stream.html.twig" only %}

<turbo-stream action="update" target="user-admin-form">
    <template>
        <h1>
            Edit User {{ user.profile.username }}
        </h1>

        {{ include("admin/user/_form.html.twig", { button_label: "Update" }) }}

        {% include "admin/user/_send_validation_email.html.twig" with { user } only %}

        {% include "admin/user/_delete_form.html.twig" with { user } only %}

        <a href="{{ path("admin_user") }}">back to list</a>
    </template>
</turbo-stream>
