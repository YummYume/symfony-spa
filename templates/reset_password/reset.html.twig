{% extends "layout.html.twig" %}

{% block title %}
    Reset your password
{% endblock %}

{% block main_title %}
    Reset your password
{% endblock %}

{% block main_body %}
    <div class="flex grow flex-col items-center justify-center">
        {{ form_start(resetForm) }}
        {{ form_row(resetForm.plainPassword) }}
        {{ component("button", { content: "Reset password", submit: true }) }}
        {{ form_end(resetForm) }}
    </div>
{% endblock %}
