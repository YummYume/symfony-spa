{% include "common/stream/alert.stream.html.twig" only %}

<turbo-stream action="replace" target="{{ target }}">
    <template>
        {% include "components/table/row/form/_switch_form.html.twig" with { config } only %}
    </template>
</turbo-stream>
